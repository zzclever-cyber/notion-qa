[pytest]
# 测试配置
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 自定义标记
markers =
    unit: 单元测试
    integration: 集成测试
    benchmark: 性能基准测试
    slow: 慢速测试

# 异步支持
asyncio_mode = auto

# 覆盖率
addopts =
    --strict-markers
    --verbose
    --tb=short
