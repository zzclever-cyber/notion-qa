# ============================================================
# 企业级 RAG Agent 知识库系统 — 依赖清单
# ============================================================

# --- LangChain 生态 ---
langchain>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0

# --- 向量检索 & 嵌入 ---
faiss-cpu>=1.9.0
sentence-transformers>=3.0.0

# --- BM25 稀疏检索 ---
rank-bm25>=0.2.2

# --- BGE Reranker ---
FlagEmbedding>=1.3.0

# --- Ragas 评估框架 ---
ragas>=0.2.0
datasets>=2.20.0

# --- Redis 会话存储 ---
redis>=5.0.0
hiredis>=2.3.0

# --- Web 框架 ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12

# --- 鉴权 (JWT + 密码哈希) ---
pyjwt>=2.9.0
bcrypt>=4.2.0

# --- 文件解析 (上传管线) ---
pymupdf>=1.24.0          # PDF 解析 (import fitz)
python-docx>=1.1.0       # Word 解析

# --- 配置 & 工具 ---
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0
tenacity>=8.0.0

# --- 中文分词 (BM25) ---
jieba>=0.42.1

# --- 日志 ---
loguru>=0.7.0

# --- 数据库 ---
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0
asyncpg>=0.29.0          # PostgreSQL/Neon 异步驱动（生产）
alembic>=1.14.0

# --- 测试 ---
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
pytest-benchmark>=4.0.0
